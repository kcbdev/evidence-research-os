# PBI-007: Budget controller (hard stops)

## Directive

Enforce `max_model_calls` / `max_research_rounds` from `project.yaml` as
hard stops (spec §10, plan task 8): budget read at run start, every
model-calling node increments `calls_used`, `targeted_research`
increments `rounds_used`, and exhaustion ends the run cleanly with a
terminal "budget exhausted" outcome — never a mid-call exception.
Cover with tests that drive a stub graph to both limits. No other node
logic changes.

## Context

- Spec: `specs/phase-1-mvp/spec.md`
- Depends on: PBI-006
- Files/modules touched: `backend/app/graph/budget.py` (or nodes +
  state extension), `backend/tests/test_budget.py`
- Plane: none

## Verification

- Spec `Contracts`: forced-exhaustion test ends the run with a clean
  terminal status and a `decisions/` entry (or run record) — no traceback.
- Gate: `python -m pytest backend/tests -q` green.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
  Soft warnings instead of hard stops are a contract violation.
