# PBI-043: Source-independence clustering (post-run hook)

## Directive

Write back `Source.independence_cluster` without touching the hot path:
`tools/dedup.py` union-find over cosine similarity (threshold 0.92,
existing `Source` model field — no schema change), invoked as a
post-run hook from `final_output` (local compute, budget-free, after the
terminal decision is recorded). Clustering reads source summaries,
writes cluster ids via the store, and never blocks or alters graph
edges. Threshold is a module constant with a test pinning merge/split
behavior on fixture vectors.

## Context

- Spec: `specs/phase-3-audit-retrieval/spec.md`
- Depends on: PBI-042 (embedder seam reuse)
- Files/modules touched: `backend/app/tools/dedup.py` (new),
  `make_final_output` hook in `backend/app/graph/nodes.py`, backend tests
- Plane: `kcb/EVRSH-47` (stays In Progress to batch)

## Execution (2026-09-10)

- Implemented: `tools/dedup.py` (union-find, 0.92, canonical=min-id,
  injectable embed), final_output hook (changed-values-only writeback,
  failure → D-dedup decision, run still completes), tests incl. hook
  failure + no-stage proofs. Fixed live typo (rb→rj). End-batch review.
- Gates: units 144 passed, API 17 passed.
- Status: Done*.

## Verification

- Spec contracts: near-duplicate fixtures share a cluster, distinct ones
  do not; research event log shows no dedup node/stage.
- Backend halves green.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
