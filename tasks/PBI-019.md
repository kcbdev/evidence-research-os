# PBI-019: End-to-end MVP validation on a real topic

## Directive

Execute the MVP "done" proof (plan §1 + guide §7 checklist) against one
real question from an existing `/areas/` exploration — no synthetic
topic: full 13-node path → pause → approve → `report.md`; deliberate
judge==council misconfiguration refused; `EvidenceTraceModal` trace
accurate; budget stop clean; cache reuse confirmed on re-run;
git-per-write visible in the Lab Project log. Fix every gap found in
the PBI that owns it (open follow-up PBIs for anything structural).
This PBI closes only when the whole checklist is green and witnessed.

## Context

- Spec: `specs/phase-1-mvp/spec.md` (Contracts = the checklist)
- Depends on: PBI-016, PBI-017, PBI-018
- Files/modules touched: none new (validation only; fixes land in
  owning PBIs' files via follow-ups)
- Plane: `kcb/EVRSH-21`

## Verification

- Guide §7: all seven boxes checked with evidence (run IDs, commit
  hashes, screenshots of trace/approval), recorded in `plans/PROGRESS.md`.
- Human gate (mandatory): user witnesses the run and signs off —
  this is the MVP release decision.

## Refinement rule

- If reality diverges from the spec: stop — do NOT weaken the
  checklist. Open follow-up PBIs or update the spec via review.
