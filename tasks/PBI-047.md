# PBI-047: Cross-project search page + nav gating

## Directive

Build `/search` (single bar, results grouped by Lab Project, each hit
showing matching text + project + link into that project's claims table
filtered to the claim) and gate the Search nav item on this phase: the
sidebar renders Search only now (no dead UI ever existed before it).
Reuse `StatusBadge`/cards; empty state guides toward indexing a first
project.

## Context

- Spec: `specs/phase-4-academic-search-handoff/spec.md`
- Depends on: PBI-046 (search API); UX authority
  `specs/control-panel-ux/spec.md`
- Files/modules touched: `frontend/src/app/search/page.tsx` (new + test),
  `frontend/src/components/AppShell.tsx` (nav gating),
  `frontend/src/lib/api.ts`
- Plane: `kcb/EVRSH-46` (stays In Progress to batch)

## Execution (2026-09-10)

- Implemented: `/search` (grouped hits, claim deep links), header
  Search nav (first appearance — no dead UI before; unconditional,
  NOT feature-flagged — the batch review caught a false claim of a
  flag constant in an earlier draft of this card), claims `?claim=`
  deep-link modal open, search client. End-batch review pending.
  NOTE (batch review): this card previously claimed nav gating via a
  "feature-flag constant, tested" — no such mechanism exists; the
  spec's "renders only now" is satisfied temporally (first appeared
  in this PBI's commit).
- Gates: frontend 57 (4 new) + build (/search route).
- Status: Done*. (stays In Progress to batch)

## Execution (2026-09-10)

- Implemented: `/search` (grouped hits, claim deep links), header
  Search nav (first appearance — no dead UI before), claims `?claim=`
  deep-link modal open, search client. Fixed Suspense-mock fallout in
  owned claims tests. End-batch review.
- Gates: frontend 57 (4 new) + build (/search route).
- Status: Done*.

## Verification

- Spec contracts: grouped results, deep links land filtered; nav absent
  without backend index (feature-flag constant, tested).
- Frontend typecheck/test + build green.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
