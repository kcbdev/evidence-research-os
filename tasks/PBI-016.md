# PBI-016: Dashboard + lab overview UI shell

## Directive

Build the control-panel shell per guide §6.1 and DOCS Technical Spec §9
(MVP tabs
only): `frontend/src/lib/api.ts` (BASE fetch helpers + `streamRun`
EventSource wrapper with cleanup), dashboard `/` (project cards +
"New Lab Project" CTA wired to `POST /lab-projects`), lab overview
`/lab/[id]` (question, budget gauge, Claims/Runs/Output tabs —
NO Ideas/Graph/Audit tabs in MVP). Native `fetch` + `EventSource`
only. Component tests or typecheck + documented manual pass.

## Context

- Spec: `specs/phase-1-mvp/spec.md`
- Depends on: PBI-001, PBI-014
- Files/modules touched: `frontend/src/lib/api.ts`,
  `frontend/src/app/page.tsx`, `frontend/src/app/lab/[id]/page.tsx`
- Plane: none

## Verification

- Dashboard lists projects from the live API; creating a project
  through the UI appears without refresh hacks; `tsc --noEmit` clean.
- Gates: `cmd /c "npm.cmd --prefix frontend run typecheck"`,
  `cmd /c "npm.cmd --prefix frontend test"`.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
  Direct filesystem/DB access from the frontend is forbidden.
