# PBI-059: Tier B — expression conditions (simpleeval)

## Directive

Open branching without engine changes: `simpleeval` pip dependency,
`app/graph/expr_condition.py` (`make_expr_condition`, plain state
fields only — no filesystem, imports, or dunder access),
`loop_condition` on `StageSpec`, and the compiler branch (expression
string → wrapped condition; registry `loop_while` untouched and
coexisting). Malformed expressions fail at methodology save/compile
with the expression echoed, never at 2am mid-run without context (well,
with the expression attached).

## Context

- Spec: `specs/phase-5b-user-authorable-logic/spec.md`
- Depends on: PBI-053 (compiler; disjoint files from PBI-058 —
  parallel-safe with it)
- Files/modules touched: `backend/pyproject.toml` (dep),
  `backend/app/graph/expr_condition.py` (new),
  `backend/app/models/methodology.py` (loop_condition only),
  `backend/app/graph/compile.py` (branch only), backend tests
- Plane: `kcb/EVRSH-57`

## Verification

- Spec contracts: true-expression loops, false-expression falls
  through, registry conditions unaffected; sandbox probe (import/dunder
  attempts raise).
- Backend halves green.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
