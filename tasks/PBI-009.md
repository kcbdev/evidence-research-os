# PBI-009: Retrieval + fetch MCP tools

## Directive

Implement `backend/app/tools/` per guide §4.1–4.2: `grep_project`
(ripgrep wrapper returning `filename:line` hits, tool description
nudging iterative narrow→read), `fetch_url` (httpx + trafilatura),
`fetch_pdf`/`extract_pdf` (pypdf), plus `store_source` /
`retrieve_evidence` blackboard helpers over `LabProjectStore`.
Investigator-facing behavior only — no graph wiring yet. Tests use
local fixtures (no live web in gates).

## Context

- Spec: `specs/phase-1-mvp/spec.md`
- Depends on: PBI-008
- Files/modules touched: `backend/app/tools/grep_project.py`,
  `backend/app/tools/fetch.py`, `backend/app/tools/blackboard.py`,
  `backend/tests/test_tools.py`
- Plane: `kcb/EVRSH-14`

## Verification

- Spec anti-patterns: grep tool description text asserts the
  narrow→read nudge; fetch tests run against local fixture server/files.
- Gate: `python -m pytest backend/tests -q` green.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
  No live-network dependency inside the deterministic gate.
