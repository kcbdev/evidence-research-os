# PBI-082: In-context source viewer with highlighted passage

## Directive

Adopt the extraction doc's source-viewer row (UX): upgrade the
`EvidenceTraceModal` from a plain-string `text_reference` to a source-viewer
presentation — source header (title, kind, tier) with the cited passage
rendered highlighted in place within the surrounding evidence text. Where
the source format allows richer display (cached HTML/PDF text available),
show the passage in that context; where it does not, the current plain
string is the explicit graceful fallback (never a broken pane, never a
second fetch path — reuse the PBI-010 cache only). Frontend-only; no API or
store changes.

## Context

- Spec: `specs/phase-6-analog-enhancements/spec.md` (C10)
- Depends on: none (trace modal landed in PBI-017/032; disjoint from
  PBI-071–077 — parallel-safe)
- Plane: `kcb/EVRSH-78`
- Files/modules touched: `frontend/src/components/` trace modal area only,
  frontend tests

## Verification

- Spec contract C10: modal pins passage-highlight rendering against
  fixture payloads; fallback path renders the plain string by test; no new
  fetch path introduced (cache-only grep pin).
- Executable: frontend typecheck/suite/build green.

## Refinement rule

- If reality diverges from the spec (e.g. the payload lacks the text
  needed for in-place display): stop, flag for human review — do not
  invent a new endpoint silently.
