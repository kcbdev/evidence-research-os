# PBI-013: Citation audit (basic) + repair + checkpoint + final output

## Directive

Close the graph per guide §2.3: `citation_audit` (MVP scope =
existence check ONLY — assert a claim citing a missing source FAILS;
pincite/support-match explicitly deferred), `targeted_repair`
(loop back to audit), `human_checkpoint` (sets
`needs_human_approval`; graph actually pauses — no second pause
mechanism), `final_output` (report.md, references.md). Cite the
adopted `langgraph-human-in-the-loop` skill for the pause/resume
pattern. Prove pause + resume via LangGraph `update_state`/stream
resume in tests.

## Context

- Spec: `specs/phase-1-mvp/spec.md`
- Depends on: PBI-012
- Files/modules touched: `backend/app/graph/nodes.py` (4 nodes),
  `backend/app/graph/build.py` (factory wiring + audit exhaustion edge),
  `backend/tests/test_audit.py`
- Plane: `kcb/EVRSH-9`

## Verification

- Spec `Contracts`: missing-source claim FAILS audit and routes to
  repair; graph run halts at `human_checkpoint` and resumes to
  `final_output` after simulated approval; output files exist.
- Budget (owns PBI-007 remainder): seeded exhaustion mid-repair-loop
  terminates cleanly; `final_output` writes a terminal `decisions/`
  entry for every run end (including `budget_exhausted`).
- Gate: `python -m pytest backend/tests -q` green.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
  Smuggling Phase 3 audit stages into MVP expands scope illegally.
