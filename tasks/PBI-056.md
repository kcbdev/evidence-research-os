# PBI-056: methodology_id in run-start/create + picker UI + run history

## Directive

Make selection real end to end: `POST /runs` accepts `methodology_id`
(resolves explicit → project default → registry default; run uses
methodology models/budget unless the Lab Project explicitly overrides —
precedence documented in the handler docstring and surfaced in the run
response); `POST /lab-projects` accepts `methodology_id` +
`budget_overrides` per the API Reference; run records + `runs.db` carry
`methodology_id` (extend the PBI-044 ensure-columns helper); history
shows which methodology each run used; methodology dropdowns land in
the run-start dialog and `/lab/new` (filling the PBI-050 reserved
slots) via a shared `MethodologyPicker` (mode-compatibility filtered).

## Context

- Spec: `specs/phase-5-methodology-registry/spec.md`
- Depends on: PBI-054 (cutover), PBI-055 (store/API), PBI-050 (dialog
  slots) — all three, sequenced last of the trio
- Files/modules touched: `backend/app/api/runs.py`,
  `backend/app/api/lab_projects.py` (create only),
  `frontend` picker + dialog + new-lab wiring, `frontend/src/lib/api.ts`,
  tests both sides
- Plane: `kcb/EVRSH-58`

## Verification

- Spec contracts: non-default methodology changes executed nodes
  (event-log assertion); unspecified run follows set-default;
  precedence documented + tested (override beats methodology).
- All four gates green.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
