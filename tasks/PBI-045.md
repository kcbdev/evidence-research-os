# PBI-045: Evidence-graph endpoint + explorer UI + claims-filter alignment

## Directive

Close the read-model surface: `GET /lab-projects/{id}/graph`
(claim/source/evidence nodes, supports/contradicts/references edges,
optional claim-status filter restricting to matching neighborhoods) in a
new `api/graph.py` mounted in `main.py`; build `/lab/[id]/graph` as a
read-only explorer (node-link view, click → side-panel detail reusing
trace-modal data, status filter, zoom/pan, zero edit affordances); and,
in the same pass, align `GET /claims` with the API Reference
(comma-separated multi-`status`, `contradictions_only` alongside the
existing range/opposition filters — one file, sequenced internally, so
the read model is coherent once, not twice).

## Context

- Spec: `specs/phase-3-audit-retrieval/spec.md`
- Depends on: none (read-only over shipped objects; parallel-safe)
- Files/modules touched: `backend/app/api/graph.py` (new),
  `backend/app/api/claims.py` (filters only), `backend/app/main.py`
  (mount), `frontend/src/app/lab/[id]/graph/page.tsx` (new + test),
  `frontend/src/lib/api.ts`
- Plane: `kcb/EVRSH-44` (stays In Progress to batch)

## Execution (2026-09-10)

- Implemented: `api/graph.py` (supports/contradicts/references,
  status neighborhoods), claims alignment (multi-status,
  contradictions_only — owned callers migrated), SVG explorer
  (lanes, zoom, side panel, keyboard nodes, theme-token edges),
  graph client + tests. End-batch review.
- Gates: units 146, API 19; frontend 53 (4 new) + build.
- Status: Done*.

## Verification

- Spec contracts: neighborhood correctness on fixtures; explorer has no
  mutation affordance (review checklist item); filter matrix test.
- All four gates green.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
