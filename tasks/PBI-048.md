# PBI-048: Academic mode (methodology + reproducibility segment)

## Directive

Add the publication-grade tier: `make_methodology_analysis` and
`make_reproducibility_audit` node factories, wired as an extra segment
between `evidence_adjudication` and `synthesis` ONLY when mode is
`academic`; extend the `mode` literals (`ProjectMeta`, `LabProjectState`,
run-start validation) with the third value. Research/brainstorm graphs
and behavior are untouched (regression-guarded by existing gates).
Academic runs must measurably cost more calls than the same question in
research mode (assert the delta in test with mocked models).

## Context

- Spec: `specs/phase-4-academic-search-handoff/spec.md`
- Depends on: PBI-034 (mode-branch pattern; same-file chain on
  `build.py`/`nodes.py` — sequenced)
- Files/modules touched: `backend/app/models/evidence.py` (literal only),
  `backend/app/graph/nodes.py` (two factories),
  `backend/app/graph/build.py` (academic branch),
  `backend/app/api/runs.py` (validation literal), backend tests
- Plane: `kcb/EVRSH-48` (stays In Progress to batch)

## Execution (2026-09-10)

- Implemented: academic literal (meta+state), methodology_analysis
  (judge re-scores that dimension only, transcript, fail-safe parse),
  reproducibility_audit (deterministic, transcript, no spend),
  academic edge branch (nodes registered for all modes, connected for
  academic only). Owned bad-mode tests migrated to "poetry".
  End-batch review.
- Gates: units 155 passed, API 19 passed.
- Status: Done*.

## Verification

- Spec contracts: segment order asserted on event log; cost delta
  asserted; research/brainstorm regression clean.
- Backend halves green.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
