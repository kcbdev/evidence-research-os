# PBI-004: Git-per-project + commit-on-write audit trail

## Directive

Extend `LabProjectStore` per plan task 5: `Repo.init` on project
creation (reuse existing repo if present), every `_write` stages and
commits with a descriptive message (`claim: {id} -> {status}` pattern
per type). `write_meta` (project.yaml) bypasses `_write` — route it through
the same commit path, not around it. Also update
`test_layout_created`'s no-`.git` assertion: git now exists by design. Prove the Phase 0 done-criteria: create a Lab Project by
hand, write one object of each type, and show `git log` reading as
project history with one commit per write. This closes the Phase 0
"done when" (hand-created dir + lossless round-trip + audit trail).

## Context

- Spec: `specs/phase-0-scaffold/spec.md`
- Depends on: PBI-003
- Files/modules touched: `backend/app/store/lab_project.py`,
  `backend/tests/test_store_git.py`
- Plane: none

## Verification

- Spec `Contracts`: scripted scenario (create → write 8 types → `git log
  --oneline` shows 8+1 descriptive commits); `git show` of any commit
  contains exactly that object's change.
- Gate: `python -m pytest backend/tests -q` green.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
  Never batch or defer commits for performance — the trail is the point.
