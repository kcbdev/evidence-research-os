# PBI-018: Run view (SSE feed + approval modal)

## Directive

Build `/lab/[id]/runs/[runId]` per guide §6.2 and DOCS Technical Spec §9:
`RunActivityFeed` (node/agent activity + token/cost ticks from SSE),
`BudgetGauge` (calls/rounds used vs max), checkpoint-approval modal
(rendered on `human_checkpoint` events, posts to `.../approve`,
resumes the stream). Handle SSE disconnect/reconnect without
duplicating events. Other Lab Projects stay usable while one run
waits — no global blocking.

## Context

- Spec: `specs/phase-1-mvp/spec.md`
- Depends on: PBI-014, PBI-016
- Files/modules touched:
  `frontend/src/app/lab/[id]/runs/[runId]/page.tsx`,
  `frontend/src/components/RunActivityFeed.tsx`,
  `frontend/src/components/ApprovalModal.tsx`,
  `frontend/src/components/BudgetGauge.tsx`
- Plane: `kcb/EVRSH-19`

## Verification

- Spec `Contracts`: start a fixture run, watch live node transitions,
  approve at the checkpoint modal, run completes to output — all in
  the browser, no polling endpoints added.
- Gates: typecheck + frontend tests green.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
  Polling fallbacks or modal-less auto-approval are forbidden.
