# PBI-031: Dashboard + overview on shadcn

## Directive

Restyle `/` and `/lab/[id]` (all 4 tabs incl. Settings + runs
history) on shadcn: Card composition (Header/Title/Description/
Content), Table for project list and runs history, Badge for
status/mode, Button/Input/Select/Label for forms, Tabs/TabList for
the overview tabs, Empty states, Skeleton loaders, Alert for errors.
Keep every behavior: fetch shapes, create-required gate, start-run
navigation, settings save, markdown report render. Update affected
tests to the new markup with IDENTICAL behavioral assertions
(list/create/filter/navigate/save). Mobile: stack grids, scroll tabs,
44px targets.

## Context

- Spec: `specs/control-panel-ux/spec.md`
- Depends on: PBI-030
- Files/modules touched: `frontend/src/app/page.tsx`,
  `frontend/src/app/lab/[id]/page.tsx`, their tests,
  additional shadcn components as needed (table, tabs, input,
  select, empty, skeleton, alert)
- Plane: `kcb/EVRSH-31`

## Verification

- Full frontend gates green; no deleted test coverage (diff-review
  each touched test); responsive spot-check 375/768.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
  No API/backend changes; no route changes.
