# PBI-072: Consensus computation + ConsensusMeter UI

## Directive

Implement guide Tasks 47–48 (Step 2, backend half + component).
Backend: add `LabProjectStore.compute_consensus(claim)` in
`backend/app/store/lab_project.py` (supporting/opposing quality-tier weight
sums + `percent_support`, null when total weight is zero) and include its
output as `"consensus"` in `GET /lab-projects/{project_id}/claims/{claim_id}`
(`backend/app/api/claims.py`), alongside — never replacing — the existing
trace payload. Frontend: add `frontend/src/components/ConsensusMeter.tsx`
per the guide sketch (emerald/amber split bar) and render it beside the
multidimensional confidence breakdown in the EvidenceTraceModal and the
Claims table row — the rigorous view stays; this is the at-a-glance one.

## Context

- Spec: `specs/phase-6-analog-enhancements/spec.md` (C2)
- Depends on: none (disjoint files from PBI-071/073; parallel-safe opener)
- Plane: `kcb/EVRSH-81`
- Files/modules touched: `backend/app/store/lab_project.py` (one method),
  `backend/app/api/claims.py` (detail payload only),
  `frontend/src/components/ConsensusMeter.tsx` (new),
  trace modal + Claims table integration, frontend/backend tests

## Verification

- Spec contract C2: a claim with known supporting/opposing tiers returns
  exact weights + percent; tier-less claim returns null percent; UI tests
  pin the meter rendering against real payloads in both surfaces.
- Executable: backend halves + frontend typecheck/suite/build green.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
