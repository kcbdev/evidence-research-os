# PBI-060: Tier C — custom_nodes/ discovery (trusted code)

## Directive

Open real algorithms under explicit single-operator trust:
`app/graph/custom_nodes.py` (`discover_custom_nodes()` — each file
defines `NODE_ID: str` + `run(state)`; malformed files fail graph-build
loudly with filename + reason, never silently skipped),
`get_full_node_registry()` merge (custom → Tier C → built-in order per
spec shadowing rule), and one illustrative `custom_nodes/` example kept
minimal. Document the guardrails in-repo: `# TOOL-LAYER:` comment
convention (no tool-layer bypass), git-revertability, budget
read/write discipline, and the second-author trigger for sandboxing
(WASM/container — deferred, flagged, not built).

## Context

- Spec: `specs/phase-5b-user-authorable-logic/spec.md`
- Depends on: PBI-053 (compiler; disjoint files — parallel-safe with
  PBI-058/059)
- Files/modules touched: `backend/app/graph/custom_nodes.py` (new),
  `backend/app/graph/compile.py` (registry merge only),
  `custom_nodes/` (new dir + example), backend tests
- Plane: `kcb/EVRSH-56`

## Verification

- Spec contracts: example node executes when named; broken file errors
  loudly with filename; no-sandboxing documented honestly (review
  checklist item, not a test).
- Backend halves green.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
