# PBI-012: Adversarial review + adjudication + synthesis

## Directive

Implement `adversarial_review` (Skeptic rubric per spec §3 — surfaces
weaknesses into the state, never edits claims silently),
`evidence_adjudication` (judge resolves every claim status from the
evidence graph only; re-assert `validate_model_assignment` at this
node's entry), and `synthesis` (draft `output/report.md` from
adjudicated claims). Tests use fixture evidence graphs with a known
correct adjudication (including a 3-agents-agree-but-unsupported trap
claim that MUST NOT resolve SUPPORTED).

## Context

- Spec: `specs/phase-1-mvp/spec.md`
- Depends on: PBI-008, PBI-011
- Files/modules touched: `backend/app/graph/nodes.py` (3 nodes),
  `backend/tests/test_adjudication.py`
- Plane: none

## Verification

- Spec `Contracts`/anti-patterns: trap-claim test resolves
  INSUFFICIENT_EVIDENCE or weaker despite unanimous council support;
  synthesis draft cites only adjudicated claims.
- Gate: `python -m pytest backend/tests -q` green.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
  Consensus-weighing adjudication is a correctness violation.
