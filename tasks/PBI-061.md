# PBI-061: Phase-5b witness (author + run Tier A/B and Tier C)

## Directive

Prove authorability end to end, human-gated like PBI-019: author one
new methodology using only Tier A/B (custom role + loop_condition, zero
files under `custom_nodes/`) and one using a Tier C custom node; run
both against a real Lab Project; confirm budget-controller and
tool-output-cache parity with built-ins (custom calls consume budget,
custom fetches hit the URL/DOI-keyed cache — asserted from budget state
and cache hit counters, not vibes). Record the witness in
`plans/PROGRESS.md`. Review type is manual (new authoring surface +
live verification).

## Context

- Spec: `specs/phase-5b-user-authorable-logic/spec.md`
- Depends on: PBI-058, PBI-059, PBI-060 (all tiers)
- Files/modules touched: witness methodologies + custom node (fixtures
  or repo files per Tier C convention), `plans/PROGRESS.md` record
- Plane: `kcb/EVRSH-61` (stays In Progress to batch)

## Execution (2026-09-11)

- Authored (no engine changes): `methodologies/witness-tier-ab-v1.yaml`
  (Tier A red-team role + Tier B loop_condition, zero custom_nodes
  files) and `methodologies/witness-tier-c-v1.yaml` (+ Tier C
  experiment_scorer stage). Both compile + stream custom stages to
  pause under mocks (new test). Marked non-default, delete after.
- Gates: units 196 passed, API 22 passed (mocked pre-proof only).
- Review type: MANUAL — live runs need a key + human judgment.
- Status: In Review. Plane: `kcb/EVRSH-61`.

## How to witness (human, needs OPENROUTER_API_KEY)

1. Backend + frontend up, open any research project with real models.
2. Start run A with methodology `witness-tier-ab-v1` (dialog picker);
   start run B with `witness-tier-c-v1`. Watch both to done via
   approvals (or run headless and approve via API).
3. Confirm: custom stages (`red_team_review`, `experiment_scoring`)
   in both event logs; `debates/red_team_reviewer.md` and
   `debates/experiment_scores.md` written; budget `calls_used` grew
   by the custom calls (parity — custom spend is charged); rerun one
   and confirm tool-output cache hits (no refetch).
4. Record result in `plans/PROGRESS.md`; delete the two witness
   YAMLs (or keep as examples); mark Done.

## Verification

- Spec contracts + done-condition (manual, human-confirmed): both runs
  complete, parity checks green, witness recorded.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
