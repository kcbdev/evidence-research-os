# PBI-052: Approval edit-and-continue

## Directive

Complete the approval trio from the API Reference: `POST …/approve`
accepts `decision: edit` with `edited_content` (422 on other shapes),
resuming the paused run with the edited synthesis draft taking effect
downstream (resume payload flows into the synthesis node input, not
around it). Extend the run-detail approval card with the Edit option +
diff-style editor on the pending draft. Reject/approve paths unchanged.

## Context

- Spec: `specs/phase-4-academic-search-handoff/spec.md`
- Depends on: none (extends shipped approve; same-file chain on `runs.py`
  after PBI-044 — sequenced)
- Files/modules touched: `backend/app/api/runs.py` (edit branch only),
  synthesis resume-input handling (`nodes.py` factory only),
  `ApprovalCard`/run-detail edit UI, tests both sides
- Plane: `kcb/EVRSH-52` (stays In Progress to batch)

## Execution (2026-09-10)

- Implemented: approve edit branch (draft replace + debates copy +
  D-edit, validate-before-record ordering kept), modal Edit draft
  editor + Save & continue, client edited_content, run view treats
  edit like approve. Interpretation recorded: edit applies to the
  pending draft post-synthesis (re-running synthesis would discard
  the edit). Witnessed in-test: edited content is what ships.
  End-batch review.
- Gates: units 157, API 21; frontend 66 (1 new) + build.
- Status: Done. Reviewed (end-batch, agentic): CHANGES-REQUESTED →
  fixes applied (non-string 422, post-done invariant, copy-first
  order, tampered-edit test, modal copy+guard test) + regressed.
  Gates green.

## Verification

- Spec contracts: edited content observably changes downstream output
  (witnessed, not mocked — test asserts the resumed synthesis consumed
  the edit).
- All four gates green.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
