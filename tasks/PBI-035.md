# PBI-035: Brainstorm skeptic rubric + idea lifecycle in graph

## Directive

Make brainstorm runs review ideas instead of citations. Add a
`skeptic-brainstorm.md` prompt (novelty / falsifiability / informativeness
of null results — never citations), extend the prompt loader with a
mode-aware rubric selection (`get_rubric(mode)` semantics), and switch
the `adversarial_review` factory to the brainstorm rubric when
`state["mode"] == "brainstorm"`, writing verdicts back onto the Idea
(status `under_skeptic_review`, novelty detail, experiment critique).
Brainstorm runs must never write claims, evidence, or sources.

## Context

- Spec: `specs/phase-2-brainstorm/spec.md`
- Depends on: PBI-034 (mode branch + novelty_check must exist; same-file
  chain on `nodes.py` — sequenced, never parallel)
- Files/modules touched: `backend/app/agents/prompts/skeptic-brainstorm.md`
  (new), `backend/app/agents/prompts.py` (rubric selection),
  `backend/app/graph/nodes.py` (adversarial factory mode switch only),
  backend tests
## Execution (2026-09-10)

- Implemented: `skeptic-brainstorm.md` prompt, mode-aware `get_skeptic_rubric`,
  `_brainstorm_adversarial_review` writing verdicts onto Ideas (status
  under_skeptic_review, no claim/evidence/source writes).
- Tests: rubric selection, file existence, topology split (behavioral),
  full brainstorm lifecycle, restart-preserves-mode — all pass.
- Review: adversarial subagent review passed; gates: backend units 109,
  API half 17 passed. Review type: agentic.
- Status: Done. Plane: `kcb/EVRSH-36`.

## Verification

- Spec contracts: skeptic output references novelty/falsifiability;
  zero claim/evidence/source writes on brainstorm runs (assert via store
  listing in test).
- Backend halves green; new tests: rubric selection by mode, idea status
  transitions proposed → under_skeptic_review, brainstorm run artifact
  census (ideas only).

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
