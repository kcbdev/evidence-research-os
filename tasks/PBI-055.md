# PBI-055: Methodology store + API (CRUD + set-default)

## Directive

Persist and serve methodologies: `store/methodology.py`
(`MethodologyStore`: list/get/save/get_default/set-default over
`methodologies/*.yaml`, git-tracked with the repo) and
`api/methodologies.py` (`GET/POST /methodologies`, `GET
/methodologies/{id}`, `PUT /methodologies/{id}` full update,
`POST /methodologies/{id}/set-default` unsetting the previous holder
for the same modes), mounted in `main.py`. POST/PUT run full
validation: unknown node/condition names and judge-overlap return 422
naming the field. `set-default` confirms at the UI layer (PBI-057);
the API itself is unconditional.

## Context

- Spec: `specs/phase-5-methodology-registry/spec.md`
- Depends on: PBI-053 (schema; disjoint files from PBI-054 —
  parallel-safe with it)
- Files/modules touched: `backend/app/store/methodology.py` (new),
  `backend/app/api/methodologies.py` (new), `backend/app/main.py`
  (mount), backend tests
- Plane: `kcb/EVRSH-55` (stays In Progress to batch)

## Execution (2026-09-10)

- Implemented: methodology CRUD API (list/get/post/put/set-default;
  201/409/422-naming/404), save-time registry validation (names,
  exclusivity, overlap — compile re-checks), tmp-store test seam
  (committed YAMLs never mutated in tests). End-batch review.
- Gates: units 168 passed, API 21 passed.
- Status: Done. Reviewed (end-batch, agentic): no must-fixes beyond
  the batch items fixed under PBI-056 (seam, subset test). Gates green.

## Verification

- Spec contracts: 3 seeded methodologies round-trip; 422 cases name the
  field; set-default flips exactly one holder.
- Backend halves green.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
