# PBI-075: Claim/idea dedup reports

## Directive

Implement guide Task 52 (Step 4). In `backend/app/tools/dedup.py`,
generalize the union-find-over-cosine-similarity into `cluster_objects`
(threshold 0.90, with the guide's stated rationale: restatement is less
likely near-verbatim than source mirroring, so the cutoff sits below the
0.92 source threshold — keep `cluster_sources` behavior byte-identical).
Add `dedup_claims`/`dedup_ideas` writing duplicate-cluster reports through
the store (one write = one commit; never add fields to the Claim/Idea
schemas). Run both off the critical path on the same post-run cadence as
the Phase 3 source clustering (extend the `final_output` hook pattern —
non-fatal by design: clustering failure records a decision, never flips a
completed run).

## Context

- Spec: `specs/phase-6-analog-enhancements/spec.md` (C5)
- Depends on: PBI-074 (same-file chain on `nodes.py` hook site —
  sequenced, never parallel)
- Plane: `kcb/EVRSH-73`
- Files/modules touched: `backend/app/tools/dedup.py`,
  `backend/app/store/lab_project.py` (report writer),
  `backend/app/graph/nodes.py` (`final_output` hook only),
  `backend/tests/test_dedup.py` (+ new tests)

## Verification

- Spec contract C5: near-duplicate ideas from separate brainstorm fixtures
  cluster together; claims likewise; source clustering output unchanged
  (existing tests green unmodified); hook failure stays non-fatal by test.
- Executable: backend halves green.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
