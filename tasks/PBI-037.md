# PBI-037: Ideas board UI + Phase-2 witness

## Directive

Build the brainstorm surface: `/lab/[id]/ideas` kanban (columns
proposed / under_skeptic_review / promoted_to_claim / rejected; cards
show statement, novelty badge, falsification-condition line), card-click
detail panel (full experiment, novelty-against list, status-change
buttons incl. Promote → offers immediate research-mode run), empty state
("No ideas yet — run a Brainstorm session", CTA to Overview). Render the
Ideas tab only when the project's mode history includes brainstorm.
Follow with the Phase-2 manual witness: real brainstorm run → idea
survives Skeptic review → UI promote → research run starts — recorded in
`plans/PROGRESS.md`.

## Context

- Spec: `specs/phase-2-brainstorm/spec.md`
- Depends on: PBI-036 (ideas API); UX pattern authority
  `specs/control-panel-ux/spec.md` (StatusBadge colors, dark-first,
  AppShell tabs)
- Files/modules touched: `frontend/src/app/lab/[id]/ideas/page.tsx`
  (new + test), `frontend/src/lib/api.ts` (ideas calls),
  overview/lab layout tab gating
## Execution (2026-09-10)

- Implemented: ideas board (`/lab/[id]/ideas` kanban + detail + promote
  offer), Overview ideas tab (gated: brainstorm mode or ideas>0),
  `getIdeas`/`patchIdea` client. Adversarial review CHANGES-REQUESTED
  (8 must-fixes) → all applied + regressed (`1dc4499`), re-review
  APPROVE.
- Gates: typecheck clean, frontend 44 passed (4 new), build green with
  the route.
- Review type: MANUAL — the Phase-2 witness (live brainstorm run →
  skeptic survival → UI promote → research run) needs a human in the
  browser. Code is done; witness outstanding.
- Status: In Review. Plane: `kcb/EVRSH-37`.

## How to review / witness (human)

1. Start backend (`uv run uvicorn app.main:app`) + frontend
   (`npm run dev`), open a brainstorm-mode project (or set mode).
2. Start a brainstorm run from Overview; watch run detail to
   `awaiting_approval`; approve → done.
3. Open the Ideas tab → board shows ideas with novelty badges +
   falsification lines; Enter-key opens a card (a11y check).
4. Open an `under_skeptic_review` idea → Promote to Claim → offer
   appears → Start research run → lands on a research run detail.
5. Expected: ideas-only writes (no claims pre-promote), no 404s,
   errors surface as Alerts. Record result in `plans/PROGRESS.md`,
   then mark Done.

## Verification

- Spec contracts + done-condition witness (manual, human-confirmed).
- `cmd /c "npm.cmd --prefix frontend run typecheck"`,
  `cmd /c "npm.cmd --prefix frontend test"`, build green; responsive
  375/768 evidence as established.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
