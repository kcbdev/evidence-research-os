# PBI-030: shadcn foundation + AppShell + theme

## Directive

Run `shadcn init` (v4 CLI) in `frontend/` (Tailwind v4 CSS-first,
dark-first OLED tokens per spec: bg #0F172A, slate scale, green-500
accent; light tokens retained for no-regression) and add components:
button, card, badge, separator, skeleton, label. Build `AppShell`
(brand link to `/`, breadcrumb slot, uniform responsive container)
and mount it in `app/layout.tsx` so every route inherits it; keep
all 5 routes' CONTENT untouched (pure shell wrap — pages render
identical output inside the new frame). Docs: `npx shadcn@latest
docs <component>` before use. Tests: shell render test (brand link,
breadcrumb slot, children). No behavior changes.

## Context

- Spec: `specs/control-panel-ux/spec.md`
- Depends on: none (foundation; PBI-031/032 build on it)
- Files/modules touched: `frontend/components.json`,
  `frontend/src/components/ui/*` (generated), `frontend/src/app/layout.tsx`,
  `frontend/src/components/AppShell.tsx`, `frontend/src/app/globals.css`
  (theme tokens), shell test
- Plane: `kcb/EVRSH-30`

## Verification

- `tsc`, `vitest run` (existing suite untouched-green), `next build`.
- Grep review: no `dark:` color overrides, no space-x/y in new code.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
  Do not restyle page content here — shell only.
