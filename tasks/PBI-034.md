# PBI-034: Ideator agent + novelty_check + mode wiring

## Directive

Make brainstorm mode execute. Add the Ideator agent (prompt file +
`propose()` function following the `agents/client.call_model_resilient`
pattern), a `make_novelty_check` node factory (Skeptic-model judgment of
novel/adjacent/duplicate against existing idea statements — no
embeddings), and branch `build_graph` on a new `mode` parameter:
brainstorm runs `… → independent_first_pass → novelty_check →
adversarial_review → …` (skipping extraction/conflict), research keeps
the exact Phase-1 topology. Validate `mode` in `POST /runs` (422 on
unknown values) and thread it into `get_graph` so the cached graph
matches the requested mode. Add `ideator` to the run-start model surface
as a fourth council key covered by `validate_model_assignment`.

## Context

- Spec: `specs/phase-2-brainstorm/spec.md`
- Depends on: none (Phase-1 behavior must not change; verified by existing gates)
- Files/modules touched: `backend/app/agents/prompts/ideator.md` (new),
  `backend/app/agents/ideator.py` (new), `backend/app/graph/nodes.py`
  (novelty_check factory only), `backend/app/graph/build.py` (mode branch),
  `backend/app/api/runs.py` (mode validate + graph key), backend tests
- Plane: `kcb/EVRSH-34`

## Verification

- Spec contracts: brainstorm run writes Ideas with falsification
  conditions + novelty checks; research topology byte-identical.
- `uv run pytest tests/ -q -p no:cacheprovider --ignore=tests/test_api_runs.py`
  and `uv run pytest tests/test_api_runs.py -q -p no:cacheprovider`
  (workdir `backend/`); new tests: novelty duplicate/adjacent/novel
  classification, mode-branch edge assertion, 422 on bad mode, ideator
  judge-overlap refusal.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review (mode
  branching must not alter research behavior — any research-path diff is
  a stop signal, not a fix-forward).
