# PBI-062: Model selector on /lab/new (OpenRouter list + defaults)

## Directive

Replace the five free-text model inputs on `/lab/new` with a shared
`ModelSelector` combobox backed by the live OpenRouter `/models` list
(24h localStorage cache + refresh), prefilled with the witnessed
default set. Degrade honestly: fetch failure still allows typed ids;
unknown ids submittable. Keep labels/aria names stable for the owned
tests. No backend changes.

## Context

- Spec: `specs/model-selector/spec.md`
- Depends on: PBI-050 (new-lab flow), PBI-056 (create payload)
- Files/modules touched: `frontend/src/lib/openrouter.ts` (new),
  `frontend/src/components/ModelSelector.tsx` (new + test),
  `frontend/src/app/lab/new/page.tsx`,
  `frontend/src/app/lab/new/page.test.tsx`
- Plane: `kcb/EVRSH-62`

## Verification

- Spec contracts above; typecheck + full frontend suite + build green.
- RTL: filter narrows, keyboard select fills id, offline fallback
  accepts typed ids, create posts selected ids.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
