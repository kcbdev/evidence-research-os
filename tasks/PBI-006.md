# PBI-006: Graph state + checkpointer + skeleton (classifier, plan)

## Directive

Implement `backend/app/graph/state.py` (`LabProjectState` per guide
§2.1, including `escalate` / `audit_passed` / `needs_human_approval`
flags) and `backend/app/graph/build.py` per guide §2.2: per-project
`SqliteSaver` checkpointer, all 13 nodes registered with the exact edge
topology from the guide (escalation branch, contradiction loop, audit
loop, `interrupt_before=["human_checkpoint"]`). Implement ONLY the
`trigger_classifier` (default escalate, TODO heuristic) and `plan`
(write `plan/research-plan.yaml` via Scientist stub) nodes — the rest
are registered stubs returning state unchanged. Cite the adopted
`langgraph-persistence` skill for checkpointer wiring.

## Context

- Spec: `specs/phase-1-mvp/spec.md`
- Depends on: PBI-002, PBI-003
- Files/modules touched: `backend/app/graph/state.py`,
  `backend/app/graph/build.py`, `backend/app/graph/nodes.py` (2 real
  nodes + 11 stubs), `backend/tests/test_graph_build.py`
- Plane: `kcb/EVRSH-6`

## Verification

- Spec `Contracts` (partial): compiled graph streams
  START→classifier→plan→…→END on a fixture project; checkpoint SQLite
  file appears beside the project YAML; `interrupt_before` registered
  (assert on compiled graph config).
- Gate: `python -m pytest backend/tests -q` green.

## Refinement rule

- If reality diverges from the spec (e.g. LangGraph version API drift):
  stop, flag for human review; the 13-node topology is not negotiable
  without a spec update.
