# PBI-076: Tournament ranking + Ranked Ideas view

## Directive

Implement guide Tasks 53–55 (Step 5). Backend: add `elo_score: float =
1200.0` to `Idea` in `backend/app/models/evidence.py` (default keeps old
YAMLs readable); new `backend/app/agents/tournament.py` with `elo_update`
(k=32) and `run_tournament` (pairwise judging via model call, injectable
RNG + judge seams per Spec D6); a `tournament_ranking` node that runs only
when the current run holds 2+ ideas, otherwise a passthrough, wired through
the registry/compiler extension points into the brainstorm pipeline after
`novelty_check`, before `adversarial_review`. Extend `GET
/lab-projects/{id}/ideas` with `elo_score` in the response and `?sort=elo`.
Frontend: add a Kanban | Ranked view toggle to the Ideas board — Ranked is a
flat `elo_score`-desc list with identical card content, no columns.

## Context

- Spec: `specs/phase-6-analog-enhancements/spec.md` (C6, D6)
- Depends on: PBI-074 (registry/compiler wiring sequencing — never parallel
  on the wiring files)
- Plane: `kcb/EVRSH-80`
- Files/modules touched: `backend/app/models/evidence.py` (one field),
  `backend/app/agents/tournament.py` (new),
  `backend/app/graph/nodes.py` + `registry.py` (node + wiring),
  `backend/app/api/ideas.py` (elo + sort),
  Ideas board + tests (backend + frontend)

## Verification

- Spec contract C6: 4+ fixture ideas produce the pinned Elo ordering via
  injected judge/RNG (no live calls); ideas endpoint sorts by elo; board
  toggle renders both views with identical cards in tests.
- Executable: backend halves + frontend typecheck/suite/build green.

## Refinement rule

- If reality diverges from the spec (e.g. brainstorm-pipeline wiring needs
  a compiler redesign under the methodology system): stop, flag for human
  review.
