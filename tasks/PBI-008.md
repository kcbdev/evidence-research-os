# PBI-008: Agent prompts + model config + judge exclusion check

## Directive

Implement `backend/app/agents/` per guide §3: OpenRouter client
(`client.py`, OpenAI-compatible endpoint, `OPENROUTER_API_KEY`),
`validate_model_assignment` in `config.py` (raises on judge∩council
overlap — called at graph-build time, before any node executes), and
the four role prompts as files (`prompts/`: Scientist, Investigator,
Skeptic, Judge per guide §3.3 wording). Prove the load-bearing
contract: a test that deliberately misconfigures judge==council and
asserts the run refuses to start.

## Context

- Spec: `specs/phase-1-mvp/spec.md`
- Depends on: PBI-007
- Files/modules touched: `backend/app/agents/client.py`,
  `backend/app/agents/config.py`, `backend/app/agents/prompts/*.md`,
  `backend/tests/test_agents.py`
- Plane: `kcb/EVRSH-8`

## Verification

- Spec `Contracts`: misconfiguration test raises before any node
  executes; per-role model IDs loadable from `project.yaml`.
- Gate: `python -m pytest backend/tests -q` green. No live
  OpenRouter calls in tests (mock the client boundary).

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
  Downgrading the check to a warning/convention is forbidden.
