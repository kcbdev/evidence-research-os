# PBI-001: Repo scaffold + uv toolchain

## Directive

Create the repository skeleton per guide §0 and standardize on `uv`:
`backend/` tree (`app/main.py`, `config.py`, `models/`, `store/`,
`graph/`, `agents/`, `tools/`, `api/`, `tests/`) with
`backend/pyproject.toml` (requires-python >=3.12, full dep list from
guide §0); `frontend/` via `create-next-app --typescript --tailwind --app`
with no data-fetching library; `lab-projects/` dir + gitignore rules
(`lab-projects/`, `.index/`, `__pycache__/`, `.venv/`, `node_modules/`,
`.next/`). Install `uv` on this machine (official installer) and prove
`uv sync` reproduces the backend env. Do NOT implement models, store,
nodes, routers, or pages — structure + installable deps only.

## Context

- Spec: `specs/phase-0-scaffold/spec.md`
- Depends on: none
- Files/modules touched: `backend/pyproject.toml`, `backend/app/*/__init__.py`
  + stubs, `frontend/` (generated), `lab-projects/.gitkeep` (dir placeholder only —
  never real data), `.gitignore`, `AGENTS.md` §1/§3 (record uv + final gate commands)
- Plane: none (EVRSH has no Todo issues; seed is empty)

## Verification

- Spec `Contracts`: `uv sync` from clean state reproduces env;
  `python -m pytest tests/ -q` and `backend/tests` collect;
  `frontend/` builds (`npm.cmd --prefix frontend run build`).
- Scope check: no domain logic exists yet — only skeleton + deps.

## Refinement rule

- If reality diverges from the spec (e.g. dep unresolvable on Python 3.12):
  stop, flag for human review; update the spec only via the spec-update path.
