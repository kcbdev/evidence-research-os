# PBI-079: Outcome-Contract coherence + end-to-end scenario (human-gated)

## Directive

Witness the system against `DOCS/Evidence-Research-OS-Outcome-Contract-v1.md`
after PBI-078 passes. Work contract §1–§5 row by row with the stated
verification method for each (real `/areas/` topic claim trace; brainstorm
promote-to-claim; one constructed claim per audit failure mode; default
methodology `models` actually filled — the known likeliest gap; two-methodology
time-travel divergence; no-code-builder methodology run; Tier A/B/C exercise;
badge/meter consistency; judge-exclusion refusal via the real API incl.
Meta-Reviewer overlap; 1:1 adjudication/decision parity; git-legible DISPUTED
reconstruction; non-goal restraint checks). Then run the §6 eleven-step
end-to-end scenario (Brainstorm with Tier A role → Skeptic survival →
tournament → promote → builder-built Research run → coverage gap →
parallel resolution → Meta-Reviewer loop-back → audit → approval → output →
cross-project search). Record every row + step in `plans/PROGRESS.md`.
Review type is manual; stays In Review until the human signs.

## Context

- Spec: `specs/phase-6-analog-enhancements/spec.md` (C8)
- Depends on: PBI-078 (cumulative validation green first)
- Files/modules touched: `plans/PROGRESS.md` (evidence record); witness
  fixtures only
- Plane: `kcb/EVRSH-79`

## Verification

- Spec contract C8: every contract row + scenario step evidenced with no
  workarounds; human sign-off in PROGRESS before Done.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review — a
  workaround fails the step's phase, it does not close this card.
