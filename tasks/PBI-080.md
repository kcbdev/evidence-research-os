# PBI-080: Search-scope filtering + per-run tool gating (Blocked)

## Directive

Ship the deferred halves of guide Tasks 49 and 58: filter retrieval results
by `search_scope` (`academic_only` → academic sources, `peer_reviewed_only`
→ systematic_review/primary_paper kinds) and disable web-search tool
availability per run when `pinned_sources` is set. No new publishing
infrastructure; keep the `fetch_url(url) -> str` signature a drop-in seam.

## Context

- Spec: `specs/phase-6-analog-enhancements/spec.md` (C3, D2)
- Depends on: PBI-073 (fields exist); BLOCKED on input — no web-search
  provider exists in the codebase (standing backlog since Phase 1) and
  `methodology.tools.enabled` is schema-only with no runtime gating seam.
  Writing filtering against a guessed provider would violate "never guess
  the external format" (PBI-051 precedent), so no code is written under
  this card until the provider lands.
- Files/modules touched (on unblock): search provider module, run-start →
  retrieval plumbing, `frontend/src/components/RunStartDialog.tsx` if the
  contract changes, tests
- Plane: `kcb/EVRSH-77`

## Verification

- Spec contract C3 (full): a `peer_reviewed_only` run excludes
  non-peer-reviewed results by test; a pinned run makes zero web-search
  tool calls by counter. Review type is manual (human judgment on provider
  choice).

## Refinement rule

- If reality diverges from the spec: stop, flag for human review — never
  guess the provider interface.
