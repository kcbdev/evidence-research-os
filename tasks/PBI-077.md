# PBI-077: Time-travel run debugging

## Directive

Implement guide Tasks 56–57 (Step 6). Backend: add read-only
`GET /lab-projects/{project_id}/runs/{run_id}/checkpoints/{node_id}` in
`backend/app/api/runs.py` returning that node's channel values from the
 project's LangGraph checkpointer (404 when absent; resolve the per-project
checkpoint path exactly the way the run subsystem does — no new storage, no
new write path, purely additive read access). Frontend: make each
`RunActivityFeed` entry clickable, opening a Sheet with the full state JSON
at that transition (formatted `<pre>` or read-only CodeMirror JSON —
reuse the builder's CodeMirror dependency; no second JSON-viewer library
per the spec anti-patterns).

## Context

- Spec: `specs/phase-6-analog-enhancements/spec.md` (C7)
- Depends on: PBI-073 (same-file chain on `backend/app/api/runs.py` —
  sequenced, never parallel)
- Plane: `kcb/EVRSH-82`
- Files/modules touched: `backend/app/api/runs.py` (one endpoint),
  `frontend/src/components/RunActivityFeed.tsx`, run view wiring,
  backend + frontend tests

## Verification

- Spec contract C7: fixture run with recorded checkpoints returns exact
  per-node state via the endpoint; feed entries open the Sheet with
  matching JSON in tests; endpoint writes nothing (assert store mtime /
  no new commits in test).
- Executable: backend halves + frontend typecheck/suite/build green.

## Refinement rule

- If reality diverges from the spec (e.g. per-node state is not
  recoverable from the as-built checkpointer without new storage): stop,
  flag for human review.
