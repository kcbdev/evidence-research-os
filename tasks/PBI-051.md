# PBI-051: labs.kcb.ma handoff mapping (human-pushed)

## Directive

Build the export bridge, not a pipeline: read the `kcb-labs` Keystatic
implementation spec (the format authority — its project-entry schema
rules over any assumption here), write `export_labs_entry(project_id)`
mapping `output/report.md` + `product/*.yaml` notes into that schema,
cover it with fixture tests (mapping correctness, no manual
reformatting needed), document the human push steps, and perform one
real push with human confirmation (recorded in `plans/PROGRESS.md`).
No new publishing infrastructure.

## Context

- Spec: `specs/phase-4-academic-search-handoff/spec.md`
- Depends on: PBI-049 (output + product notes must exist)
- Files/modules touched: `backend/app/tools/labs_export.py` (new),
  docs note, backend tests (fixtures only — the push itself is manual)
- Plane: `kcb/EVRSH-50` (stays In Progress to batch)

## Execution (2026-09-10)

- BLOCKED on input, by the card's own refinement rule: the `kcb-labs`
  Keystatic implementation spec (the declared format authority) is not
  in this repo — it lives in the operator's KCB Labs area, which the
  agent cannot see. Writing the mapping against a guessed schema would
  violate "never guess the external format", so no code was written.
- To unblock: paste the Keystatic project-entry schema (or a pointer
  to the spec file) and the PBI resumes at `export_labs_entry` +
  fixture tests + push docs. The push itself stays human either way.
- Status: Blocked.

## Verification

- Spec contracts: fixture mapping test green; human-confirmed push
  recorded. Review type is manual (human judgment + live external push).

## Refinement rule

- If reality diverges from the spec (e.g. Keystatic schema drift):
  stop, flag for human review — never guess the external format.
