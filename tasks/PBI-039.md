# PBI-039: citation_verify tool + 3-stage audit node + results store

## Directive

Replace the MVP existence-by-id audit with the real 3-stage check.
New `tools/citation_verify.py`: `AuditResult(stage, status, detail)` +
`check_existence` (httpx HEAD via the existing `cached_fetch` tool-output
cache — never raw HTTP), `check_pincite` (section lookup in fetched
text), `check_support_match` (auditor model call; auditor resolves
settings-fallback → env `AUDITOR_MODEL`; unset → WARNING-with-reason,
never silent PASS). Rewrite the `make_citation_audit` factory to audit
every (claim, evidence) pair, persist per-stage rows to `audits/*.yaml`
(new store methods `write_audit_run/list_audit_runs`, git-committed like
all objects), and set `audit_passed` only when no FAIL exists. Repair
node unchanged.

## Context

- Spec: `specs/phase-3-audit-retrieval/spec.md`
- Depends on: PBI-038 (auditor model resolution)
- Files/modules touched: `backend/app/tools/citation_verify.py` (new),
  `backend/app/graph/nodes.py` (audit factory only),
  `backend/app/store/lab_project.py` (audit methods only), backend tests
- Plane: `kcb/EVRSH-39`

## Verification

- Spec contracts incl. the done probe: support_match-FAIL vs
  existence-FAIL distinction (fixture sources + mocked fetch/auditor —
  real results, never faked).
- Backend halves green; new tests: three stages unit-tested, node writes
  rows + sets flag, unset-auditor degrades to WARNING.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review (an
  unmeetable probe means unfinished, not relaxed).
