# PBI-065: Libraries UI (skills / prompts / roles / tools pages)

## Directive

Build the four plain-CRUD library pages the canvas depends on — no
canvas, no React Flow in this PBI. Routes: `/settings/skills` (Card
grid: name, description, using-roles + editor with Name/description
and `@uiw/react-md-editor` body; assigned-roles shown read-only —
assignment lives in the Role editor), `/settings/prompts` (Table:
name, description, last-edited, used-by-count + editor with generous
Textarea and per-save version history in an Accordion with
Revert-per-entry), `/settings/roles` (Card grid with referencing-
methodology badges + Dialog editor: Name/ID, Description, system
prompt as Prompts-library Combobox OR inline Textarea with "Save as
reusable prompt" Checkbox, Tools Checkbox grid, searchable Model
combobox reusing the `ModelSelector` pattern, output-schema Select,
 Skills multi-select (Checkbox grid — waived from "multi-Combobox"
 at execution with rationale in the Execution record below), `/settings/tools` (read-only Table: name,
description, source MCP server, with header text stating new tools
are a backend/MCP concern). All pages use `react-hook-form + zod`
(client schemas mirroring the PBI-063 Pydantic models), native
`fetch` against `/api/v1` via `lib/api.ts` (new exported functions
only — this PBI owns the `api.ts` library-section additions),
shadcn composition rules from the control-panel-ux spec (semantic
tokens, `cn()`, titled Dialogs), and RTL tests per page. Honor the
"one source of truth" relationship: role↔skill assignment is editable
only from the Role editor side.

## Context

- Spec: `specs/nocode-methodology-builder/spec.md`
- Depends on: PBI-063 (library APIs), PBI-064 (deps + shadcn
  primitives). Disjoint files from PBI-066+ (new route dirs).
- Files/modules touched: `frontend/src/app/settings/skills/**`,
  `frontend/src/app/settings/prompts/**`,
  `frontend/src/app/settings/roles/**`,
  `frontend/src/app/settings/tools/**` (all new + tests),
  `frontend/src/lib/api.ts` (append library functions only)
- Plane: `kcb/EVRSH-70`

## Verification

- Spec contracts: each page CRUDs against the real backend (seed one
  entry per library, reload, entry persists; prompt re-save creates a
  version and Revert restores it); tools page has no create/edit
  affordance; role editor's inline-prompt Checkbox promotes to the
  Prompts library in the same action.
- Executable: `cmd /c "npm.cmd --prefix frontend run typecheck"`,
  `cmd /c "npm.cmd --prefix frontend test"`, `next build` green.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review
  (e.g. a PBI-063 endpoint shape forces a UI compromise — do not
  reshape the backend inside this PBI; file the delta against PBI-063).

## Execution (2026-09-12)

- Implemented: 4 pages + 15 tests (skills 4, prompts 5, roles 4,
  tools 2), api.ts library section (ids encodeURIComponent'd;
  savePrompt takes server-managed-fields-free NewPrompt).
- Adversarial review CHANGES-REQUESTED → all must-fixes applied:
  Tooltip on card Edit buttons (spec contract); 9-field client
  schema mirror; New-Prompt duplicate-id guard; promote
  link-don't-clobber + half-state disclosure; library-pick
  divergence clears provenance with inline note; htmlFor→button
  labels rewired; model-label orphan fixed; Why? touch target;
 badge keys by id; N+1 ref failures surfaced; identity-guarded
 editor init (roles + prompts); exact-URL + edit-path + ordering
 + revert-refresh test assertions; MDEditor mock passes through
 aria-labelledby (proves the real label link); F1 combobox→grid
 waiver: checkbox grid chosen over multi-Combobox (equivalent
 multi-select for small sets, fully keyboard accessible, trivially
 testable; combobox portals add focus risk with zero functional
 gain) — disclosed here and in the close-out report for sign-off.
- Gates: typecheck clean, 99/99 vitest green, `next build` green
  with 20 routes (4 new, all static — SSR mount guard proven).
- Status: Done (review: agentic — see Plane comment for evidence).
