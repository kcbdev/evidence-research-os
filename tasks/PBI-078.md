# PBI-078: Phase-6 cumulative validation (human-gated)

## Directive

Execute the guide's 7-row cumulative validation table against live models,
one row at a time, recording evidence in `plans/PROGRESS.md`: (1)
targeted_research wall-clock sublinearity; (2) consensus meter correctness
+ `peer_reviewed_only` run behavior per PBI-073's implemented scope;
(3) coverage-check task surfacing + Meta-Reviewer catch of an incoherent
draft; (4) cross-run duplicate idea clustering; (5) tournament Elo ordering
stability across repeats; (6) time-travel state fidelity; (7) pinned-sources
run making zero discovery writes + visibly multi-angle plan. Per Spec D4,
then RE-RUN the Phases 1–5 done-conditions (the Outcome Contract requires
it — the guide's "no re-validation" line is superseded). Any row needing a
workaround fails its underlying phase regardless of checklists. Review type
is manual (live spend + human judgment); this card stays In Review until the
human signs the table.

## Context

- Spec: `specs/phase-6-analog-enhancements/spec.md` (C1–C7, D4)
- Depends on: PBI-071, PBI-072, PBI-073, PBI-074, PBI-075, PBI-076, PBI-077
  (all Phase-6 behavior must land first)
- Files/modules touched: `plans/PROGRESS.md` (evidence record); witness
  fixtures only — no product-code changes expected (defects found here file
  follow-up PBIs, they do not balloon this card)
- Plane: `kcb/EVRSH-76`

## Verification

- Spec contracts C1–C7 witnessed live + Phases 1–5 done-conditions
  re-green, all recorded; human sign-off in PROGRESS before Done.
- Executable: deterministic gates re-run green at validation time.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review — a
  workaround here is a phase failure, not a shortcut.
