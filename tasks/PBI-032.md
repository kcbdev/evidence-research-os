# PBI-032: Claims + trace dialog on shadcn

## Directive

Restyle `/lab/[id]/claims` and `EvidenceTraceModal` on shadcn: Table
(sticky first column inside scroll-x container on mobile),
Dialog + DialogTitle (replacing the hand-rolled overlay; keep
Escape/backdrop/aria-label behavior), Badge (opposition),
Progress (confidence bars — one per dimension, values kept),
Alert (empty-evidence copy + errors), Skeleton (trace loading).
Keep: filters→refetch, sort toggles, keyboard row buttons,
field-for-field trace content, dangling-source fallback. Update
tests to new markup with identical assertions (add DialogTitle
assert). Mobile: table scrolls, modal full-screen-ish.

## Context

- Spec: `specs/control-panel-ux/spec.md`
- Depends on: PBI-030
- Files/modules touched:
  `frontend/src/app/lab/[id]/claims/page.tsx`,
  `frontend/src/components/EvidenceTraceModal.tsx`,
  `frontend/src/components/ClaimConfidenceBar.tsx`,
  `frontend/src/components/ContradictionBadge.tsx`,
  their tests, shadcn dialog/table/progress/alert/skeleton
- Plane: `kcb/EVRSH-32`

## Verification

- Full frontend gates green; coverage preserved per-file;
  responsive spot-check 375/768.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
  No API/backend changes.
