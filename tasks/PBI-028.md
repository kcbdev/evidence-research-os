# PBI-028: Per-project model settings UI

## Directive

Live-fire follow-up: UI-created projects get fail-closed
`openrouter/auto` model defaults (correct — runs refuse until
configured), but NOTHING in the UI configures them, so Start-run
fails with a refusal the user cannot resolve in-app. Add model
configuration to the control panel: a settings section on the lab
overview (council scientist/investigator/skeptic + judge, OpenRouter
ID strings with the current values prefilled), persisted via a new
`PATCH /api/v1/lab-projects/{id}` endpoint that updates ONLY the
model assignment (plus budget caps if cheap — same shape) through
`store.write_meta` (committed, validated: judge-overlap refused
with a readable 400 the UI surfaces verbatim per the api.ts
error-body fix). Tests: endpoint validation + refusal case; UI test
that saving posts the assignment.

## Context

- Spec: `specs/phase-1-mvp/spec.md` (settings surface; Tech Spec
  `/settings` page defers full per-role config here)
- Depends on: PBI-014 (routers), PBI-016 (overview)
- Files/modules touched: `backend/app/api/lab_projects.py`,
  backend API tests, `frontend/src/app/lab/[id]/page.tsx`,
  frontend tests
- Plane: `kcb/EVRSH-28`

## Verification

- Configure → start run succeeds where auto/auto refused; overlap
  config refused with readable error; gates green (backend split
  halves, tsc, vitest, build).

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
  Never persist secrets (no API keys in project.yaml — models only).
