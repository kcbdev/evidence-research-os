# PBI-057: Methodologies settings UI (list + YAML editor + preview)

## Directive

Build `/settings/methodologies` (list: name, description, compatible
modes, default badge, Set-as-default with confirm) and
`/settings/methodologies/[id]` (raw YAML editor with highlighting,
validate-on-save against the Pydantic schema with inline errors, plus
read-only compiled-stage preview as a vertical list with loop-back
arrows). New Methodology starts from a minimal valid template (research-
compatible default pipeline skeleton), never a blank file.

## Context

- Spec: `specs/phase-5-methodology-registry/spec.md`
- Depends on: PBI-055 (methodology API)
- Files/modules touched: `frontend/src/app/settings/methodologies/**`
  (new + tests), `frontend/src/lib/api.ts`
- Plane: `kcb/EVRSH-60` (stays In Progress to batch)

## Execution (2026-09-10)

- Implemented: methodologies list (badges, confirm set-default),
  YAML editor + validate-on-save inline errors + approximate stage
  preview, new-from-template, header Methodologies link. yaml npm dep
  adopted. End-batch review.
- Gates: frontend 72 (8 new) + build (3 routes).
- Status: Done*.

## Verification

- Spec contracts: save-invalid shows field errors, nothing persists;
  preview matches saved YAML stages; set-default flips the badge.
- Frontend typecheck/test + build green.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
