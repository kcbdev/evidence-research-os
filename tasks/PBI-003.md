# PBI-003: Filesystem store + CRUD helpers

## Directive

Implement `backend/app/store/lab_project.py` per guide §1.2:
`LabProjectStore` creates the full subdirectory layout, exposes typed
`write_*/read_*/list_*` accessors per object type, and is the ONLY
module allowed to write object YAML. Tests cover write/read/list for
every type plus layout creation on a tmp path. Do NOT add git behavior
here (PBI-004) — plain filesystem persistence only.

## Context

- Spec: `specs/phase-0-scaffold/spec.md`
- Depends on: PBI-002
- Files/modules touched: `backend/app/store/lab_project.py`,
  `backend/tests/test_store.py`
- Plane: `kcb/EVRSH-5`

## Verification

- Spec `Contracts`: write→read→list round-trip for all 8 object types
  on an isolated tmp dir; no other module writes YAML (grep proves it).
- Gate: `python -m pytest backend/tests -q` green.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review;
  never hand-edit object YAML outside the helpers to "make a test pass".
