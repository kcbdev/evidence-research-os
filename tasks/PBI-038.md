# PBI-038: Global settings fallback API (models + budget)

## Directive

Add the lab-root settings fallback the later phases resolve against:
`GET/PUT /settings/models` (roles scientist/investigator/skeptic/judge/
ideator/auditor; save runs the run-start `validate_model_assignment`
check plus auditor-outside-rotation check, 422 with the field named) and
`GET/PUT /settings/budget` (max calls/rounds/sources/sources-per-claim).
New `store/settings.py` persisting one JSON/YAML file at lab root
(untracked runtime config, same class as `runs.db` — never inside a Lab
Project repo). Mount a `settings` router in `main.py` at `/api/v1`
alongside the project-scoped routers.

## Context

- Spec: `specs/phase-3-audit-retrieval/spec.md`
- Depends on: none (new files only — parallel-safe with everything)
- Files/modules touched: `backend/app/store/settings.py` (new),
  `backend/app/api/settings.py` (new), `backend/app/main.py` (mount),
  backend tests
- Plane: `kcb/EVRSH-40`

## Verification

- Spec contracts: 422 shapes, auditor exclusion enforced, round-trip
  persistence.
- Backend halves green; new tests: get defaults, put valid, judge
  overlap 422, auditor-in-rotation 422, budget round-trip.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
