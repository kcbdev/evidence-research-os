# PBI-046: Shared cross-project semantic index + GET /search

## Directive

Promote the PBI-042 index to shared: single LanceDB instance at
`<lab-root>/.shared-index/lancedb` with `project_id` as filterable
column, per-project writers repointed to it, one-time backfill of
existing per-project tables (documented, idempotent — rerun safe), and
`GET /api/v1/search?q=&limit=` returning project_id/title/claim_id/
matching_text/score. Root `.gitignore` covers `.shared-index/` (verify,
like `.index/`). Per-project Tantivy indices stay separate (no
speculative cross-project keyword search).

## Context

- Spec: `specs/phase-4-academic-search-handoff/spec.md`
- Depends on: PBI-042 (per-project index + seam)
- Files/modules touched: `backend/app/tools/semantic_index.py`
  (connection path + backfill), `backend/app/api/search.py` (new),
  `backend/app/main.py` (mount at `/api/v1`), backend tests
- Plane: `kcb/EVRSH-42` (stays In Progress to batch)

## Execution (2026-09-10)

- Implemented: shared LanceDB table (+watermarks, predicate guard),
  scoped/cross search split, backfill fn + __main__, GET /search
  (422 empty, limit clamp, title enrichment), index_evidence_unit
  removed (lazy subsumes it), gitignore verified. End-batch review.
- Gates: units 149 passed, API 19 passed.
- Status: Done*.

## Verification

- Spec contracts incl. the search probe: query returns a seeded hit from
  a different project than the queried context.
- Backend halves green.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
