# PBI-054: Capture 3 methodologies + delete hardcoded builder

## Directive

Execute the one-time migration as a single atomic PBI (capture-then-
delete, never two — no coexisting pipeline definitions):
`scripts/capture_default_methodology.py` emits
`methodologies/deep-research-council-v1.yaml` (default, research),
`brainstorm-ideation-v1.yaml` (novelty_check branch + ideator), and
`academic-publication-v1.yaml` (academic segment); capture parity is
diff-reviewed against the hardcoded topology (node order, edges,
loop-backs, budget short-circuits, single human_checkpoint interrupt);
then delete `build_graph` from `graph/build.py` and cut `runs.py`
`get_graph` to compile via `build_graph_from_methodology`, rekeying the
graph cache on (project, methodology_id, methodology content-hash) so
YAML edits can never serve a stale pipeline. Existing tests adapt to
the compiler path; no run-behavior change.

## Context

- Spec: `specs/phase-5-methodology-registry/spec.md`
- Depends on: PBI-053 (compiler)
- Files/modules touched: `scripts/capture_default_methodology.py` (new),
  `methodologies/*.yaml` (new), `backend/app/graph/build.py` (deletion),
  `backend/app/api/runs.py` (cutover + cache key), backend tests
- Plane: `kcb/EVRSH-54`

## Verification

- Spec contracts: parity diff reviewed; stale-cache test (edit YAML →
  next run uses new pipeline); full backend halves green (strongest
  regression signal in this phase).

## Refinement rule

- If reality diverges from the spec: stop, flag for human review (a
  fork here is the failure mode — do not leave both builders alive).
