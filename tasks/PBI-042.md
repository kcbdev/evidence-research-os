# PBI-042: LanceDB + fastembed semantic index (per-project)

## Directive

Add Tier-3 retrieval: `lancedb`, `fastembed`, `numpy` pip dependencies,
`tools/semantic_index.py` (BAAI/bge-small-en-v1.5 local CPU embedder,
per-project table with id/vector/text/project_id, `index_evidence_unit`
+ `semantic_search`), same mtime-checked lazy discipline as PBI-041
(never inline on writes), wired into the investigator's tool surface
alongside keyword search. Embedding calls go through one seam so tests
inject vectors without downloading the model; live first-run download
is documented in the module docstring (CPU/ONNX, no API cost).

## Context

- Spec: `specs/phase-3-audit-retrieval/spec.md`
- Depends on: none (disjoint files from PBI-041 — parallel-safe)
- Files/modules touched: `backend/pyproject.toml` (deps),
  `backend/app/tools/semantic_index.py` (new), investigator call-site,
  backend tests (mocked embedder)
- Plane: `kcb/EVRSH-43`

## Verification

- Spec contracts: semantic hit on paraphrased fixture query; no model
  download in tests (assert seam used).
- Backend halves green.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
