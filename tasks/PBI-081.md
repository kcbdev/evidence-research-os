# PBI-081: Audit pass-rate quality metric (Phase 4 tracking)

## Directive

Adopt the extraction doc's quality-over-volume row (Performance): surface
citation-audit pass rate as the literal quality metric instead of
vanity counts. Backend: extend the audits read model
(`backend/app/api/audits.py` + store as needed) with a per-run (and
project-latest) summary — counts by stage (existence/pincite/support_match)
and status (PASS/WARNING/FAIL) plus an overall pass rate; pure derivation
from stored `AuditRun` rows, no schema change. Frontend: render the summary
on the Audits view (and link it from the run view where audits surface).
Words-per-report and citations-per-claim are never presented as quality
signals wherever this summary appears.

## Context

- Spec: `specs/phase-6-analog-enhancements/spec.md` (C9)
- Depends on: none (audits API/UI landed in PBI-040; disjoint from
  PBI-071–077 — parallel-safe)
- Plane: `kcb/EVRSH-74`
- Files/modules touched: `backend/app/api/audits.py` (+ store helper if
  needed), Audits view, backend + frontend tests

## Verification

- Spec contract C9: fixture audit runs yield exact stage/status counts +
  rate; UI pins the summary rendering; no vanity metric is labeled as
  quality in the touched views (grep-pinned).
- Executable: backend halves + frontend typecheck/suite/build green.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
