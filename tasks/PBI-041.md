# PBI-041: Tantivy keyword index + investigator wiring

## Directive

Add Tier-2 retrieval: `tantivy` pip dependency,
`tools/keyword_index.py` (schema id/body, `rebuild_index` over
claims/evidence/ideas YAML into `<project>/.index/tantivy/`,
`keyword_search`), lazy rebuild discipline (rebuild when the index is
older than the newest source YAML — same no-invalidation-protocol rule
as the claims.db view, never inline on writes), and wire
`keyword_search` into the investigator's tool surface so retrieval
iterates narrow → read → narrow again across ripgrep + BM25.

## Context

- Spec: `specs/phase-3-audit-retrieval/spec.md`
- Depends on: none (new files + investigator call-site; parallel-safe
  with PBI-038/039/040 — declare order after PBI-040 only for witness
  sequencing, not files)
- Files/modules touched: `backend/pyproject.toml` (dep),
  `backend/app/tools/keyword_index.py` (new), investigator retrieval
  call-site in `backend/app/graph/nodes.py`, backend tests
  (fixture-built index, no network)
- Plane: `kcb/EVRSH-41`

## Verification

- Spec contracts: seeded document found for obvious query; stale-index
  rebuild proven by mtime test.
- Backend halves green.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
