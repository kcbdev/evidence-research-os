# PBI-074: Reasoning additions (coverage_check + Meta-Reviewer + perspective planning)

## Directive

Implement guide Tasks 50, 51, and 59 (Steps 3 and 7) as one graph-reasoning
increment. Backend, in `backend/app/graph/nodes.py`: (a) a `coverage_check`
node (unused-evidence → follow-up Task with `reason="coverage_check"`,
assigned scientist) placed between `evidence_adjudication` and `synthesis`
with the conditional edge feeding gaps back into `targeted_research`;
(b) a `meta_review` node after `synthesis`, before `citation_audit`, with
the `META_REVIEWER_SYSTEM` prompt in `backend/app/agents/prompts.py` and a
conditional loop back to `synthesis` on failure; (c) the Task 59
`SCIENTIST_PLANNING_ADDENDUM` appended to the existing plan prompt (no new
node). Extend the PBI-008 judge-exclusion hard check (`validate_model_assignment`)
to also reject Meta-Reviewer/council model overlap, including the deliberate-
misconfiguration refusal test (Spec D5). Register and wire all nodes through
the existing registry/compiler extension points — no compiler redesign.

## Context

- Spec: `specs/phase-6-analog-enhancements/spec.md` (C4, D5)
- Depends on: PBI-071 (same-file chain on `nodes.py` — sequenced, never
  parallel)
- Plane: `kcb/EVRSH-75`
- Files/modules touched: `backend/app/graph/nodes.py` (new nodes + edges),
  `backend/app/graph/registry.py` (registration only),
  `backend/app/agents/prompts.py` (two prompt additions),
  `backend/app/agents/config.py` (exclusion extension),
  `backend/tests/` (node behavior + refusal tests)

## Verification

- Spec contract C4: incomplete fixture run surfaces a coverage task that
  routes to targeted_research; incoherent draft fixture loops synthesis via
  meta_review; judge==council and meta_reviewer==council both refuse at
  run start; plan output shows multi-perspective decomposition.
- Executable: backend halves green.

## Refinement rule

- If reality diverges from the spec (e.g. wiring needs compiler/storage
  changes): stop, flag for human review.
