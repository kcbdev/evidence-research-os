# PBI-044: Run retry from checkpoint + run-record schema evolution

## Directive

Add explicit recovery: `POST /lab-projects/{id}/runs/{run_id}/retry`
resumes a `failed` (or `interrupted` — checkpoints survive processes,
threads do not) run under the SAME run_id/thread from its last
checkpoint (run==thread contract kept, no new id, continuation events
appended); 400 for any other status. Extend `runs.db` with `started_at`,
`mode`, `duration_s` columns via an `ensure-columns` helper (migrate,
never wipe PBI-029 history), surface them in run get/list, and show the
retry action on failed/interrupted run views.

## Context

- Spec: `specs/phase-3-audit-retrieval/spec.md`
- Depends on: PBI-029 (done — builds on its persistence, same-file chain
  on `runs.py`)
- Files/modules touched: `backend/app/api/runs.py` (retry + columns),
  run-view retry affordance (frontend), backend tests
- Plane: `kcb/EVRSH-45` (stays In Progress to batch)

## Execution (2026-09-10)

- Implemented: ensure-columns generalized (mode/started_at/duration_s),
  `_ensure_graph` shared by approve+retry, POST retry (same id, 400
  otherwise), started_at/duration in get+list, run-view Retry button
  for failed/interrupted. Fixed owned list-shape test. End-batch review.
- Gates: units 144, API 19; frontend 49 (1 new) + build.
- Status: Done. Reviewed (end-batch, agentic): CHANGES-REQUESTED →
  fixes applied (interrupted-retry test, references guard, error-clear
  assert) + regressed. Gates green.

## Verification

- Spec contracts: failed → retried → done under one id; interrupted →
  retried works; 400 otherwise; old runs.db migrates with history intact.
- Backend halves green.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review (never
  silently resume — retry is an explicit named action or it does not
  exist).
