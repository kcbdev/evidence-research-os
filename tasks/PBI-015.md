# PBI-015: Claims/decisions/budget/report API + SQLite view

## Directive

Implement the read-model routers per guide §5.3 and spec §8:
`rebuild_claims_index` (regenerates `.index/claims.db` from
`claims/*.yaml` — never hand-maintained), `GET .../claims`
(status + min_confidence filters against the view),
`GET .../claims/{id}` (claim + linked evidence + sources),
`GET .../decisions`, `GET .../budget`, `GET .../output/report`.
Include the new routers in `main.py` (shared file with PBI-014 —
hence sequenced after, not parallel).

## Context

- Spec: `specs/phase-1-mvp/spec.md`
- Depends on: PBI-003, PBI-014
- Files/modules touched: `backend/app/api/claims.py`,
  `backend/app/main.py` (router includes),
  `backend/tests/test_api_claims.py`
- Plane: none

## Verification

- Spec rationale: filtered query (status=DISPUTED, confidence<0.5)
  returns exactly the fixture's matching claims — the case grep can't do.
- Gate: `python -m pytest backend/tests -q` green.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
  Hand-maintaining the index instead of regenerating is forbidden.
