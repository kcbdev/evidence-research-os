# PBI-033: Run view on shadcn + responsive/a11y sign-off

## Directive

Restyle `/lab/[id]/runs/[runId]`, `RunActivityFeed`, `BudgetGauge`
(Progress bars), `ApprovalModal` → shadcn Dialog (dossier list,
buttons, note field; keep approve/reject/note semantics, busy guard,
resolved-ref behavior EXACTLY). Then the pass-wide sign-off: full
responsive sweep 375/768/1024/1440 (document per-route notes),
pre-delivery checklist from the skill (no emoji icons in touched
files — replace badge ⚠ with Lucide, focus visible, reduced-motion,
keyboard paths, 44px targets), fix what it finds. Keep ALL run-view
behaviors (stream/reset/resubscribe/resting-mount/completion).

## Context

- Spec: `specs/control-panel-ux/spec.md`
- Depends on: PBI-031, PBI-032
- Files/modules touched: run view page + 3 components + tests,
  shadcn dialog/progress (+ lucide-react if not present — icon
  package only, no design deps)
- Plane: `kcb/EVRSH-33`

## Verification

- Full frontend gates green; coverage preserved; responsive notes
  recorded in PROGRESS; checklist signed line-by-line.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
  No API/backend changes; no behavior changes whatsoever.
