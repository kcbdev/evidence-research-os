# PBI-002: Pydantic evidence models

## Directive

Implement `backend/app/models/evidence.py` exactly per guide §1.1:
`Source` (all 9 `SourceKind` values, `quality_tier` 1–9 validated,
`independence_cluster` optional), `Claim` (all 7 statuses,
5-dimension `Confidence`, `adjudicated_by`), `Evidence`, `Idea`,
`Task`, `Decision`, `BudgetState` (with `exhausted()`), `ProjectMeta`
(`council_models` dict + `judge_model`). Unit tests prove every object
YAML from spec §4.2 round-trips losslessly. No store, graph, or API code.

## Context

- Spec: `specs/phase-0-scaffold/spec.md`
- Depends on: PBI-001
- Files/modules touched: `backend/app/models/evidence.py`,
  `backend/tests/test_models.py`
- Plane: `kcb/EVRSH-3`

## Verification

- Spec `Contracts`: hand-created Lab Project YAML for every §4.2 type
  round-trips (`model_validate` → `model_dump` → identical YAML).
- Gate: `python -m pytest backend/tests -q` green.

## Refinement rule

- If reality diverges from the spec (e.g. a §4.2 field cannot round-trip):
  stop and flag — the spec is the contract, fix the code, not the schema.
