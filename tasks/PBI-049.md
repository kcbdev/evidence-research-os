# PBI-049: Product notes API + Output + Decisions pages

## Directive

Ship the deliverable surface: `ProductNote` model + store methods
(`product/` dir exists — add `write_product_note/list_product_notes`,
git-committed) + `POST/GET /lab-projects/{id}/product-notes`
(linked_area optional slug); `/lab/[id]/output` (rendered markdown +
TOC sidebar, export markdown, empty state pre-first-run, promote-to-
product form posting to the new endpoint); `/lab/[id]/decisions`
(reverse-chron what/why list, read-only). Align `GET output/report` to
`{markdown, generated_at}` (mtime-derived — the one safe envelope
alignment; no client depends on the old key).

## Context

- Spec: `specs/phase-4-academic-search-handoff/spec.md`
- Depends on: none (new backend files + new pages; parallel-safe)
- Files/modules touched: `backend/app/models/evidence.py` (model only),
  `backend/app/store/lab_project.py` (product methods only),
  `backend/app/api/product.py` (new), `backend/app/api/claims.py`
  (report envelope only — sequenced internally),
  `backend/app/main.py` (mount), `frontend/src/app/lab/[id]/output/page.tsx`
  + `decisions/page.tsx` (new + tests), `frontend/src/lib/api.ts`
- Plane: `kcb/EVRSH-49` (stays In Progress to batch)

## Execution (2026-09-10)

- Implemented: ProductNote model/store/API (201, N-ids, empty→422),
  report envelope {markdown, generated_at}, /output (TOC, export,
  promote form), /decisions (read-only log), overview output tab now
  links out. Owned envelope test + dead import migrated. End-batch
  review.
- Gates: units 157, API 19; frontend 61 (6 new) + build.
- Status: Done. Reviewed (end-batch, agentic): CHANGES-REQUESTED →
  fixes applied (history columns, reverse-chron, stale copy) +
  regressed. Gates green.

## Verification

- Spec contracts: note round-trip, export downloads, promote flow,
  empty states, report envelope.
- All four gates green.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
