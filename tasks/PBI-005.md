# PBI-005: Coolify services + data volume

## Directive

Create the Coolify project per guide §0/plan task 2: `backend` service
(uvicorn, port 8000) + `frontend` service (Next.js, port 3000) +
persistent volume mounted at `/data/lab-projects` with
`LAB_PROJECTS_ROOT=/data/lab-projects` on the backend. Backend may
serve a placeholder response at this stage (real routers are Phase 1).
Infra-only PBI — no application code changes.

## Context

- Spec: `specs/phase-0-scaffold/spec.md`
- Depends on: PBI-001
- Files/modules touched: Coolify project config only (no repo files
  except optional `docker/*` if the chosen deploy path needs them —
  prefer Coolify-native builds)
- Plane: `kcb/EVRSH-2`

## Verification

- Spec `Contracts`: both services healthy in Coolify; a file written
  under the volume path survives a backend redeploy (mount proof).
- Manual step (human gate): open both service URLs, confirm reachability.

## Refinement rule

- If reality diverges from the spec (e.g. volume mount path differs on
  the target host): stop, flag for human review — the mount path is a
  load-bearing contract for every later PBI.

## Execution (2026-09-11, deploy evidence.kcb.ma)

- Blocker: Coolify MCP has no create tools — project/apps/volume must
  be click-created in the UI (human). Agent prepared everything else.
- Added (this PBI's allowed `docker/` scope): `backend/Dockerfile`
  (py3.12-slim + uv, `--locked` frozen sync, healthcheck on
  `/api/v1/lab-projects`, `LAB_PROJECTS_ROOT=/data/lab-projects`),
  `frontend/Dockerfile` (node:22 multi-stage standalone, API URL via
  build arg), both `.dockerignore`s, `output: "standalone"` in
  `next.config.ts` (verified locally — `.next/standalone/server.js`
  builds).
- DNS verified: `evidence.kcb.ma` + `api.evidence.kcb.ma` both resolve
  to the host. Plan: frontend → `https://evidence.kcb.ma`, backend →
  `https://api.evidence.kcb.ma` (no path-routing subtleties).
- Backend env: `LAB_PROJECTS_ROOT=/data/lab-projects` (image default),
  `OPENROUTER_API_KEY` (secret, human sets), `FRONTEND_URL`
  = `https://evidence.kcb.ma`. Volume → `/data/lab-projects`.
  Frontend build arg: `NEXT_PUBLIC_API_URL=https://api.evidence.kcb.ma`.
- Status: In Review (manual: UI creation + secrets + mount proof).
