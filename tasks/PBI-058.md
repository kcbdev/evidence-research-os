# PBI-058: Tier A — custom roles via prompt+tools (no code)

## Directive

Open methodology authorship without engine changes, tier 1:
`app/graph/generic_node.py` (`make_prompt_agent_node(role_config)` —
ReAct over the role's tools via a new `tools/dispatch.py`
name→tool registry covering every existing tool, optional
output-schema validation, result written like any built-in node),
`CustomRoleSpec` schema addition, and compiler resolution trying
`custom_roles` ids first (shadowing a built-in logs a warning naming
both — never silent). `dispatch.py` registers existing tools only; no
new tool integrations in this PBI.

## Context

- Spec: `specs/phase-5b-user-authorable-logic/spec.md`
- Depends on: PBI-053 (compiler; disjoint files from PBI-054–057 —
  parallel-safe once 053 lands)
- Files/modules touched: `backend/app/graph/generic_node.py` (new),
  `backend/app/tools/dispatch.py` (new),
  `backend/app/models/methodology.py` (CustomRoleSpec only),
  `backend/app/graph/compile.py` (resolution order only), backend tests
- Plane: `kcb/EVRSH-59` (stays In Progress to batch)

## Execution (2026-09-10)

- Implemented: `tools/dispatch.py` (6 existing tools, documented
  exclusions), `graph/generic_node.py` (bounded 3-round ReAct,
  transcript, budget charge, schema-hint), CustomRoleSpec, compiler
  custom-first + shadow warn + compile-time tool resolution.
  End-batch review.
- Gates: units 180 passed, API 22 passed.
- Status: Done*.

## Verification

- Spec contracts: YAML-only custom role executes (event log shows the
  stage); shadowing warns; unknown tool name fails at compile with the
  name.
- Backend halves green.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
