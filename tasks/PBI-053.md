# PBI-053: Node/condition registries + Methodology schema + compiler

## Directive

Build the swappable-pipeline engine (no behavior change yet — the
hardcoded builder still serves runs): `app/graph/registry.py`
(`NODE_REGISTRY` mapping names to the existing `make_*(path)` builder
factories — adapted to the as-built factory pattern, zero-arg nodes
register directly; `CONDITION_REGISTRY` with `has_open_contradictions`
+ `audit_failed`), `app/models/methodology.py` (`Methodology`/
`StageSpec`/`WorkflowSpec`/`ToolsSpec`/`PromptsSpec`/`BudgetDefaults`
per the Phase-5 guide; `custom_roles`/`loop_condition` keys REJECTED by
validation until Phase 5b), and `app/graph/compile.py`
(`build_graph_from_methodology`: unknown names fail loudly naming the
field, loop-backs compile to conditional edges, interrupts collect,
judge-overlap validated fail-closed). Compiler output for the default
pipeline must be edge-identical to `build_graph` (differential test).

## Context

- Spec: `specs/phase-5-methodology-registry/spec.md`
- Depends on: PBI-048 (all nodes incl. academic must exist first)
- Files/modules touched: `backend/app/graph/registry.py`,
  `backend/app/models/methodology.py`, `backend/app/graph/compile.py`
  (all new), backend tests
- Plane: `kcb/EVRSH-53` (stays In Progress to batch)

## Execution (2026-09-10)

- Implemented: `models/methodology.py` (forbid-extra → 5b gate),
  `graph/registry.py` (uniform builders + route/loop predicates
  mirroring build.py), `graph/compile.py` (loud validation,
  route+loop forms, shared checkpoint file). Guide deviation
  recorded: `route` added — loop-only cannot express the real
  topology. Parity proven behaviorally, all 3 modes. End-batch review.
- Gates: units 164 passed, API 21 passed.
- Status: Done. Reviewed (end-batch, agentic): CHANGES-REQUESTED →
  loop-back loss found → loop_always schema+compiler+capture fix (see
  PBI-054) + behavioral loop tests + regressed. Gates green.

## Verification

- Spec contracts: differential edge-identity test; unknown-name errors
  name the field; overlap refused; 5b-keys rejected (proves the gate
  the witness PBI will later lift).
- Backend halves green.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
