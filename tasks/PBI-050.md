# PBI-050: New-lab flow + run-start dialog + runs history page

## Directive

Complete the run lifecycle UI with backend-ready fields only
(mode/question/budget — the methodology picker slot is reserved, see
spec): `/lab/new` single-page form (title, question, mode radio,
advanced budget toggle pre-filled from defaults, Create + Create-and-
start); run-start dialog on Overview (question override, mode default,
advanced budget override → redirect to run detail immediately); runs
history list page (`/lab/[id]/runs` table: id/timestamp, mode, status,
duration) linking to run detail. No backend changes — wire to shipped
endpoints; picker-shaped extension points are props with TODO(Phase 5),
not dead UI.

## Context

- Spec: `specs/phase-4-academic-search-handoff/spec.md`
- Depends on: none (frontend-only; parallel-safe with PBI-046–049)
- Files/modules touched: `frontend/src/app/lab/new/page.tsx` (new),
  run-start dialog component (new), `frontend/src/app/lab/[id]/runs/page.tsx`
  (new + tests), `frontend/src/lib/api.ts`
- Plane: `kcb/EVRSH-51`

## Verification

- Spec contracts: create + create-and-start redirect correctly;
  dialog posts and lands on run detail; history rows link through.
- Frontend typecheck/test + build green; 375/768 evidence.

## Refinement rule

- If reality diverges from the spec: stop, flag for human review.
