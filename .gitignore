# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
# Node
node_modules/
.next/
# Evidence Research OS derived indices (regeneratable, never committed)
.index/
.shared-index/
# Lab Project data volume (separate git remote per Lab Project, own history;
# only the .gitkeep placeholder is tracked so clones reproduce the layout)
lab-projects/*
!lab-projects/.gitkeep
# OS / editors
.DS_Store
Thumbs.db
# Local server/process logs (never committed)
*.log
# Playwright MCP snapshots (local verification artifacts)
.playwright-mcp/
